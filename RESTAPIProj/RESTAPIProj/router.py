# from RESTAPIApp.viewsets import PropertyListViewset  #import the viewset for routers
# from rest_framework import routers #import router to make the urls.

# """
# Routers are used with ViewSets in django rest framework to auto config the urls. Routers provides a simple, quick and consistent way of wiring ViewSet logic to a set of URLs. Router automatically maps the incoming request to proper viewset action based on the request method type(i.e GET, POST, etc).
# """

# router = routers.DefaultRouter()  #setting the instance to default router.
# router.register('properties',PropertyListViewset, basename='properties')  #setting the route path.

